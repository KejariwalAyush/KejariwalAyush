name: kejariwal_ayush
version: 1.0.0+1
publish_to: none
description: My Portfolio Project.
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.1
  google_fonts: ^2.3.1
  url_strategy: ^0.2.0
  velocity_x: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/
    - assets/icons/
  uses-material-design: true
